// ==================== API SERVICE (LocalStorage Based) ====================
// Tất cả dữ liệu được lưu trong localStorage, không cần backend

// Placeholder - Không sử dụng
export const api = {
  // Tất cả logic đã được chuyển sang localStorage
  // Xem các file context và data files để quản lý dữ liệu
};
